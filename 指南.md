# MCRYII 博客修改指南

本站是 Hugo 静态站（主题 PaperMod），部署在 GitHub Pages（域名 www.mcryii.fun）。
仓库根目录：`D:\Downloads\Programs\myblog-new`

## 站点架构总览

| 项目 | 内容 |
|---|---|
| 静态生成器 | Hugo 0.162.1（extended） |
| 主题 | PaperMod（git submodule） |
| 部署 | GitHub Actions 推送 `master` 自动构建 → GitHub Pages |
| 本地工具 | `blog_tool.py`（Tkinter 桌面写作助手） |
| 数据来源 | `content/` 文章 + `data/*.json` 站点数据 + `static/` 静态资源，全部构建期生成 |

**改动分工规则**：
- 改内容 → `content/`、`data/`、`static/`
- 改样式/交互 → `layouts/`、`static/css/`、`static/js/`
- `public/` 是构建产物，已 gitignore，不用手动改、不用提交

**改完如何验证**：
- 本地预览（含草稿）：`hugo server -D`，默认 http://localhost:1313
- 生产构建：`hugo --minify`（产物进 `public/`）

---

## 模块详解

### 1. 站点配置（hugo.yaml）

【功能】站点标题、域名、主页欢迎语文字、输出格式（HTML/RSS/JSON 搜索索引）。

【改哪里】`hugo.yaml`：
- `baseURL`（域名）、`title`（站点名）
- `params.homeInfoParams`（首页欢迎语 Title / Content）
- `outputs`（生成 index.json 供搜索用，别删）

【注意点】hugo.yaml 里的 `menu.main` 实际上**没被使用**（顶栏是自定义硬编码的），改菜单要去 `layouts/partials/header.html`。

### 2. 首页

【功能】全屏欢迎语（带打字机效果 + 闪烁光标）+ 下方文章卡片列表。

【改哪里】
- 欢迎语文字 → `hugo.yaml` 的 `params.homeInfoParams`
- 首页背景、欢迎语样式、打字机脚本 → `layouts/partials/extend_head.html`（`page-bg-home` 样式块 + 打字机脚本）
- 文章卡片样式 → 目前用的是主题默认列表（`themes/PaperMod/layouts/list.html`），本站没有自定义首页布局

【注意点】想改首页文章卡片样式，需要新建 `layouts/index.html` 覆盖主题默认。

### 3. 顶栏导航

【功能】三段式顶栏：站点名/头像 + 金色胶囊下拉菜单 + 右侧按钮（主题切换 Alt+T、搜索 Ctrl+K、移动端菜单）。移动端是抽屉菜单，滚动时顶栏自动隐藏。

【改哪里】
- 菜单项（增删页面、改名字）→ `layouts/partials/header.html` 最上面的 `$navItems` 字典
- 交互逻辑（下拉、抽屉、快捷键）→ `static/js/nav.js`
- 样式 → `layouts/partials/extend_head.html` 顶栏 CSS 块

【注意点】菜单是**硬编码字典**，不是 hugo.yaml 的 menu。当前菜单：主页 / 文章（文章列表、分类、归档）/ 网站导航 / 动态（动态、相册、笔记本）/ 记录（音乐、更新日志、日历、规划）/ 关于（关于我、友链、赞助、下载），"写文章"已移除。

### 4. 全站搜索

【功能】Ctrl+K 或点搜索按钮唤起弹窗，Fuse.js 模糊搜索文章标题/摘要/分类/标签，展示前 10 条结果。

【改哪里】`layouts/partials/extend_head.html` 的"搜索栏"部分（HTML + 样式 + Fuse.js 脚本）。

【注意点】搜索索引（`index.json`）是构建期生成的，新增文章后需重新构建才可搜到。Fuse.js 脚本已加 `defer` 异步加载、不阻塞首屏渲染，搜索面板打开时脚本必然已就绪。

### 5. 音乐播放器 + 音乐页

【功能】右下角悬浮播放器：自动扫描全部音乐，支持顺序/单曲循环/随机、音量/静音、进度条拖动、歌单面板、折叠成小方框；PJAX 切换页面不断播、播放位置自动记忆。音乐页 `/music/` 为**全屏沉浸式舞台**（无侧栏）：动态极光背景 + 金色粒子、旋转黑胶唱片、Web Audio 实时频谱可视化、歌词面板（`.lrc` 同步滚动高亮，**默认隐藏，点唱片封面进入歌词聚焦模式（封面淡出隐藏），点歌词区域或按 L 键返回封面**；无歌词显示"纯音乐"）、金色发光控制区、歌单侧滑抽屉、快捷键（空格播放/暂停、←/→ 快退快进、↑/↓ 音量、N/P 切歌、M 静音、L 歌词、Esc 关歌单），与播放器实时联动。播放器与音乐页自动读取 mp3 内嵌专辑封面，无内嵌封面时用默认图。

【改哪里】
- 加歌 → 把 mp3 丢进 `static/music/`，文件名格式 `作者 - 歌名.mp3`（自动拆分作者/歌名）
- 加歌词 → 同目录放同名 `.lrc` 文件（如 `作者 - 歌名.lrc`），页面自动按时间轴滚动高亮；没有 `.lrc` 的歌曲显示"纯音乐"
- 播放器 HTML/样式/逻辑 → `layouts/partials/extend_head.html` 中部（约 770–1335 行）
- 音乐页（全屏舞台样式 + 频谱/歌词/歌单抽屉/快捷键全部在此）→ `layouts/_default/music.html`

【注意点】
- 歌单是构建期扫描目录生成的，加歌后需重新构建；歌词 `.lrc` 是静态文件直接读取，加歌词后本地预览无需重建
- 播放状态存 localStorage（`hugoPlayerState` 位置/播放中、`hugoPlayerMode` 模式）
- 封面是运行时从 mp3 内嵌 ID3 标签提取的（`extend_head.html` 播放器脚本的 `extractCover` 函数），无需构建；首次播放某首歌时封面会先显示默认图、随后自动替换（每首歌只解析一次，内存缓存）
- 音乐页固定暗色风格（不随主题切换，顶栏胶囊/下拉/移动端抽屉同步改为暗色玻璃风格）并隐藏页脚，整页无白边；进入音乐页时通过 `body.music-page` 类临时隐藏右下角悬浮播放器，PJAX 切走自动恢复
- 频谱可视化基于 Web Audio API，受浏览器自动播放策略限制，需页面内点击/键盘操作一次后启动（未启动时自动降级为静态动画条）

### 6. 侧边栏（三栏布局）

【功能】全站三栏布局：
- 通用页左侧：博主信息卡（头像/文章数/分类数/标签数/社交链接）、音乐卡、站点统计（文章/分类/标签/总字数/运行天数/最后更新）
- 通用页右侧：日历卡、浏览量卡（不蒜子 UV/PV）、今日一言、最近更新 5 篇
- 文章页左侧：全部分类折叠树、"写新文章"入口、音乐卡
- 文章页右侧：目录、最近更新

【改哪里】
- 通用页 → `layouts/partials/sidebar-left.html`、`layouts/partials/sidebar-right.html`
- 文章页 → `layouts/partials/posts-sidebar-left.html`、`layouts/partials/posts-sidebar-right.html`

【注意点】音乐卡通过 `window.__mcryiiPlayer` API + `mcryii:player` 事件与播放器同步，改代码时别改这两个接口名。

### 7. 文章系统（列表页 / 详情页 / 封面）

【功能】
- 列表页 `/posts/`：首屏"最新发布"大图 + 其余文章竖排列表（滚动渐入动画）+ 分页
- 详情页：标题日期居中、金色下划线、目录、标签、上下篇导航、分享、评论
- 封面系统：文章可自定义封面，没有封面自动用默认图

【改哪里】
- 文章内容 → `content/posts/*.md`（Front Matter：title、date、categories、tags、draft、cover.image）
- 文章图片 → `static/images/`
- 列表页版式 → `layouts/posts/list.html`
- 详情页版式 → `layouts/posts/single.html`

【注意点】无封面自动用 `/images/default-cover.png`；`cover.image` 建议 5:3。本地预览时文章页头部有"✏ 在写作工具中编辑"按钮（`blogtool://` 协议，线上不显示）。全站 `static/images/` 图片已统一压缩优化（PNG 无损/量化取优、JPG quality 85，尺寸与透明度不变），覆盖文章封面、动态、笔记、相册图片；新增图片建议先压缩再放入，避免体积回涨。图片放大脚本（medium-zoom）已加 `defer`，不阻塞首屏。

### 8. 分类 / 标签

【功能】`/categories/` 卡片式分类页（每类显示图标、篇数、前 4 张缩略图、最近文章），`/tags/` 标签云。文章自动按 Front Matter 归类。

【改哪里】分类卡片样式 → `layouts/_default/terms.html`；分类/标签数据 → 直接在文章 Front Matter 里写，不用单独建文件。

【注意点】由 Hugo taxonomy 自动生成，改分类名只需改文章 Front Matter。

### 9. 归档（/archive/）

【功能】按年 → 月 → 日期列出全部文章。

【改哪里】`layouts/_default/archive.html`（样式 + 结构）；数据自动来自 `content/posts/`。

### 10. 日历（/calendar/）

【功能】三栏布局：中间大月历 + 右侧功能栏（节日·纪念日、当天日程、即将到来、添加日程）。文章日金色标记、节日红角标（emoji）、个人日程蓝点、今天高亮；点日期看当天文章/节日/日程；支持添加/删除日程、到点提醒（页面顶部提示条 + 浏览器通知，需授权）。通用页侧边栏仍保留一个小日历。

【改哪里】
- 页面结构/样式/逻辑 → `layouts/_default/calendar.html`
- 节假日/纪念日数据 → `data/events.json`（events 数组：date 用 `YYYY-MM-DD` 固定日期或 `MM-DD` 每年循环；type 用 festival=节日 / memorial=纪念日；emoji 是日历角标）
- 侧边栏小日历 → `layouts/partials/sidebar-right.html`
- 纯逻辑自检 → `scripts/test_calendar_logic.js`（`node scripts/test_calendar_logic.js`）

【注意点】
- 日历文章数据（`__calData`）构建期由 Hugo 生成，新增文章后重新构建
- 个人日程存 localStorage（键 `mcryii_calendar_schedules`），换浏览器/清缓存会丢；提醒只在打开日历页时检查（静态站无后端推送，不打开页面就不会弹）
- 农历节日（春节/端午/中秋等）硬编码在 `data/events.json` 里的公历日期，每年年初记得补一次

### 11. 动态（/dynamic/）

【功能】公告板：便签动态（文字 + 最多 4 张图片 + 链接）与文章剪报按时间合并排列；便签是贴纸风格（旋转角度、图钉装饰），图片点击可灯箱预览（支持 ← → 翻页、ESC 关闭）。

【改哪里】
- 动态数据 → `data/moments.json`（moments 数组：id、date、text、images、link）
- 动态图片 → `static/images/moments/`
- 页面结构/样式/灯箱 → `layouts/_default/dynamic.html`

【注意点】动态和文章按时间合并排序；本地预览时显示"＋发动态 / ✏ 编辑"按钮（`blogtool://` 协议，线上隐藏，编辑链接按动态 `id` 定位）；建议用写作工具的"💬 动态"页管理，会自动同步写入 `data/moments.json`（旧数据无 `id` 的会在读取时自动补齐）。

### 12. 笔记本（/notes/）

【功能】学习笔记（当前 13 篇，分 **51单片机（7 篇）+ ESP32（6 篇）** 两组）：列表按编号展示，支持分类筛选 chips、分组折叠；详情页带编辑器风格代码块。

【改哪里】
- 笔记内容 → `content/notes/*.md`（Front Matter 需 `num` 编号、`group` 分组、`description` 简介、date）
- 列表页 → `layouts/notes/list.html`
- 详情页 → `layouts/notes/single.html`

【注意点】列表按 `Params.num` 排序，`group` 决定分组。

### 13. 网站导航（/site-nav/）

【功能】个人书签管理页：`data/bookmarks.json`（当前 114 条、7 个分类）渲染书签卡片（自动抓取网站 favicon），支持实时搜索；"ACG"、"呃呃"两个分类默认隐藏，输入密码可解锁查看。本地预览时还会合并加载**本地专属书签** `static/bookmarks-local.json`（成人内容等，仅本地可见，线上不出现）。

【改哪里】
- 公开书签 → `data/bookmarks.json`（categories 数组：name + links，每链接 title/url）
- 本地专属书签 → `static/bookmarks-local.json`（已加入 `.gitignore`，不会进公开仓库；格式与 bookmarks.json 相同，本地预览时自动合并）
- 隐藏分类列表、解锁密码 → `layouts/_default/site-nav.html`（`HIDDEN_CATS` 数组、`LOCK_HASH` 是密码的 SHA-256 哈希）

【注意点】密码校验在前端 JS 里，防君子不防小人；解锁状态存 localStorage（`site-nav-unlock-v1`），换浏览器会重置。成人内容已移入本地文件，注意不要把它提交到 GitHub。

### 14. 友链（/friends/）

【功能】友链卡片网格（头像 + 名字 + 简介），当前为空，显示占位提示。

【改哪里】`data/friends.json`（friends 数组：name、url、avatar、desc）。

### 15. 更新日志（/changelog/）

【功能】时间线展示站点更新记录。

【改哪里】`data/changelog.json`（entries 数组：date、title、items 列表）。

### 16. 规划（/planning/）

【功能】年度目标看板：目标卡片按"进行中/待开始/已完成"分组，显示进度条、优先级、截止日期；待开始目标点"开始"按钮会本地切到进行中。

【改哪里】`data/planning.json`（goals 数组：title、status、progress、deadline、priority、note）；页面样式在 `layouts/_default/planning.html`。

【注意点】"开始"状态存 localStorage（`plan-started-v1`），是本地记忆，换浏览器会重置，不会同步到线上数据。

### 17. 下载页（/downloads/）

【功能】自动扫描 `static/files/` 目录，按文件夹分组，以金色卡片网格展示文件（按扩展名自动匹配图标 + 可读大小），分类可折叠（默认展开，状态本地记忆）。

【改哪里】把文件放进 `static/files/<分类目录>/` 即可自动列出；页面样式在 `layouts/_default/downloads.html`。

【注意点】构建期用 `readDir` 扫描生成，加文件后重新构建；折叠状态存 localStorage（`download-collapse-v1`），换浏览器会重置。本地预览时页面顶部有"📦 管理下载文件"按钮（`blogtool://edit?tab=downloads` 协议，跳转写作工具下载管理页签，线上隐藏）。

### 18. 留言板（/guestbook/）+ 评论系统

【功能】全站文章和留言板集成 Cusdis 评论（无后端、免费），评论区高度自动适配。

【改哪里】
- 评论组件 → `layouts/partials/comments.html`（Cusdis app-id 在这里）
- 单篇文章关闭评论 → 该文章 Front Matter 加 `comments: false`
- 留言板正文 → `content/guestbook.md`

【注意点】PJAX 切换页面时 Cusdis 有单独初始化逻辑，别和普通脚本混在一起。

### 19. 赞助（/sponsor/）

【功能】赞助说明 + 收款码占位框。

【改哪里】正文在 `content/sponsor.md`；收款码占位 → `layouts/_default/sponsor.html` 的 `.qr-placeholder`（目前是占位文字，换图片即可）。

### 20. 其他单页（关于 / 相册）

【功能】三栏布局的 Markdown 内容页。

【改哪里】
- 关于我 → `content/about.md`
- 相册 → `content/gallery.md`（图片放 `static/images/`）

【注意点】"写文章"页面已删除，写作入口改为写作工具（桌面版 / 网页版）+ 本地预览时的 `blogtool://` 编辑链接。

### 21. 页脚（一言 + 站点统计）

【功能】页脚显示一言每日语录（调用 hitokoto API，localStorage 按天缓存）+ 不蒜子访客/阅读统计。

【改哪里】`layouts/partials/extend_footer.html`（HTML + 脚本 + 样式）；样式变量可配合 `static/css/custom.css`。

【注意点】一言接口挂了有兜底文案，不影响页面。

### 22. 统计与分析（Cloudflare）

【功能】Cloudflare Web Analytics 全站埋点。

【改哪里】`layouts/partials/extend_head.html` 最顶部（`data-cf-beacon` 里的 token）。

### 23. 本地写作工具（blog_tool.py）

【功能】写作助手，两种形态：
- **桌面版**：`python blog_tool.py`（Tkinter 窗口）——文章列表管理、新建/编辑/删除文章、5:3 封面裁剪编辑器、Markdown 预览、一键插入图片、启动本地预览、一键 git 提交推送（有确认弹窗）
- **网页版**：`python blog_tool.py --web`，浏览器打开 http://127.0.0.1:8777/（仅本机可访问），支持 URL 参数直达页面（如 `?tab=moments`、`?tab=downloads`）
- **动态管理**：内置"💬 动态"页，支持发布、编辑（改文字/增删图片/改链接，发布时间保持不变）、删除；读写 `data/moments.json`，图片自动复制到 `static/images/moments/`，编辑或删除时清理不再引用的图片文件
- **下载管理**：桌面版"📦 下载"标签页 + 网页版"📦 下载"页，支持上传文件到 `static/files/<分类>/`、新建分类、删除文件；上传是复制到目标目录，重新构建后下载页自动更新

【配套文件】
- `start_blog_tool.bat` —— 启动器（可用环境变量 `BLOG_TOOL_PY` 指定 Python 路径）
- `install_blogtool_protocol.bat` —— 注册 `blogtool://` 协议（运行一次即可，让站点上的编辑按钮能唤起工具）
- `打开博客.bat` / `打开博客.ps1` —— 一键启动 Hugo 本地预览并打开浏览器；`打开博客.ps1`（桌面快捷方式指向它）每次双击都会先结束旧 hugo 进程再启动全新的，避免长驻进程的模板缓存导致顶栏等渲染异常；`打开博客.bat` 是旧版会弹黑框，一般不用
- `blog_tool_icon.ico` —— 工具图标

【注意点】开头 `BLOG_ROOT` 路径是硬编码的（`D:\Downloads\Programs\myblog-new`），换机器要改；推送前有确认对话框，符合"本地优先、禁自动推送"；`blogtool://` 编辑按钮只在本地预览（localhost）时显示；下载管理操作的是 `static/files/` 下的真实文件，删除不可恢复（有确认弹窗）；网页版上传走 base64，大文件建议用桌面版；分类名仅限字母/数字/-/_。

### 24. CI/CD 部署

【功能】推送 `master` 分支后，GitHub Actions 自动构建（`hugo --minify`）并部署到 GitHub Pages。

【改哪里】`.github/workflows/hugo.yml`（Hugo 版本 0.162.1、构建/部署步骤）。

【注意点】`public/` 已 gitignore；部署是自动的，任何推送前需用户明确批准。

### 25. 主题（PaperMod）

【功能】基础主题，提供默认布局和组件。

【改哪里】`themes/PaperMod/`（git submodule，一般不动）；本站 `layouts/` 下的同名文件会覆盖主题文件。

【注意点】更新主题 = 更新 submodule，可能影响本站覆盖的样式。

---

## 关键注意点（汇总）

1. **顶栏菜单**：硬编码在 `layouts/partials/header.html` 的 `$navItems`，hugo.yaml 的 `menu.main` 没用上。
2. **主页文章列表**：目前是主题默认布局，想改首页卡片样式需新建 `layouts/index.html` 覆盖。
3. **PJAX 局部刷新**：站内跳转只替换 `<main>` 内容，页面级脚本会重新执行；播放器状态跨页保持，别把它写进会重复执行的代码里。局部刷新请求有 5 秒超时兜底，网络卡住/失败时自动转整页跳转，且卡住期间再次点击会直接整页跳转，不会无限等待。
4. **改完要构建**：纯改 `content/`、`data/`、`static/` 后需重新 `hugo` 构建；播放器歌单、下载列表、搜索索引、日历数据都是构建期自动生成的。
5. **localStorage 记忆**：导航解锁、规划"开始"状态、播放器状态都存在浏览器本地，换设备/浏览器会重置。
6. **本地优先**：所有修改仅限本地，禁止未经批准推送 GitHub。
7. **成人内容书签在本地**：`static/bookmarks-local.json` 已被 `.gitignore` 忽略，仅本地预览合并显示；修改时注意不要把它提交到 GitHub。
