# MCRYII 博客修改指南

本站是 Hugo 静态站（主题 PaperMod），部署在 GitHub Pages（域名 www.mcryii.fun）。
项目主目录：`/home/mcryii/myblog-new`（WSL2 Ubuntu-24.04 原生环境）
Windows 访问/工作区路径：`Z:\myblog-new`（已映射本地盘符） / `\\wsl.localhost\Ubuntu-24.04\home\mcryii\myblog-new`
Windows 本地备份：`D:\Downloads\Programs\myblog-new`

## 站点架构总览

| 项目 | 内容 |
|---|---|
| 静态生成器 | Hugo 0.162.1（extended，WSL2 Linux 原生） |
| 主题 | PaperMod（git submodule） |
| 部署 | GitHub Actions 推送 `master` 自动构建 → GitHub Pages |
| 本地工具 | `blog_tool.py`（Tkinter 桌面写作助手 / 网页版） + `manage.sh`（WSL 综合管理） |
| 数据来源 | `content/` 文章 + `data/*.json` 站点数据 + `static/` 静态资源，全部构建期生成 |

**改动分工规则**：
- 改内容 → `content/`、`data/`、`static/`
- 改样式/交互 → `layouts/`、`static/css/`、`static/js/`
- `public/` 是构建产物，已 gitignore，不用手动改、不用提交
- **图标规范**：网站 UI 与网页版写作工具一律用自绘内嵌 SVG 线条图标（金色描边 `currentColor`），**禁止使用 emoji 当图标**；桌面版 Tkinter 不支持 SVG，按钮用纯文字标签。Markdown 正文里如需图标，用 `{{< icon "图标名" >}}`，图标定义在 `layouts/partials/header.html` 的 SVG sprite

**改完如何验证**：
- WSL 命令行预览：`./run.sh` 或 `./manage.sh serve`（默认 http://localhost:1313）
- Windows 快捷预览：双击 `MCRYII.lnk` 或 `打开博客.ps1` / `打开博客.bat`（自动桥接 WSL 后台启动并唤起 Windows 浏览器）
- 生产构建：`./manage.sh build` 或 `hugo --minify`（产物进 `public/`）


---

## 模块详解

### 1. 站点配置（hugo.yaml）

【功能】站点标题、域名、主页欢迎语文字、输出格式（HTML/RSS/JSON 搜索索引）。

【改哪里】`hugo.yaml`：
- `baseURL`（域名）、`title`（站点名）
- `params.homeInfoParams`（首页欢迎语 Title / Content）
- `outputs`（生成 index.json 供搜索用，别删）
- 标签页图标与 Favicon 声明 → `layouts/partials/head.html`（覆盖 PaperMod 主题默认模板，使用根相对路径并注入 `assetsVersion` 强刷缓存）
- 网站图标静态资源 → `static/favicon.ico`、`static/favicon-32x32.png`、`static/favicon-16x16.png`、`static/apple-touch-icon.png`

【注意点】hugo.yaml 里的 `menu.main` 实际上**没被使用**（顶栏是自定义硬编码的），改菜单要去 `layouts/partials/header.html`；`params.assetsVersion` 是站内 JS（`nav.js`/`global-nav.js`/`scroll-effects.js`/`dino-game.js`/`fly-game.js`）及浏览器标签页 Favicon 的缓存版本号，已在 `打开博客.ps1` 启动脚本中实现**半自动递增**（当检测到任一核心 JS 文件的修改时间新于 `hugo.yaml` 时，自动以 `YYYYMMDD-N` 递增更新版本号），也可以手动维护，避免访客使用旧缓存。全站 Favicon 已在 `layouts/partials/head.html` 中改造为带 `?v={{ site.Params.assetsVersion }}` 的站内相对路径，既杜绝了本地预览跨域读取线上旧图标的问题，也保证换图标时各浏览器能瞬间清除本地 SQLite 缓存即刻生效。`params.description`（全站 meta description 摘要）、`params.images`（默认分享卡片图 og:image，页面自带 cover 时自动用页面封面）与 `static/robots.txt` 影响 SEO/分享效果。`minify.disableHTML: true` 必须保留——Hugo 的 HTML 压缩器会把模板里 safeHTML 输出的内联 SVG 转义成可见文本（曾导致下载页图标显示源码），只禁了 HTML 压缩，CSS/JS 照常压缩。

### 2. 首页

【功能】全屏 Hero 落地页：背景为 `static/images/home-hero.jpg`（全屏 cover + 底部深色渐变遮罩，暗色主题遮罩深一档），欢迎语（打字机效果）**左下角**呈现；无文章列表。**下滑航程 → 彩蛋**：hero 下方是约 1.5 屏的滑翔旅程——三张数据卡（**建站天数**按最早文章日期算、文章数只数 `posts` 栏目、标签数全站，Hugo 模板直接算，无新依赖；口径与侧栏「站点统计」一致）+ 漂浮云朵；开始下滑后 **ω 猫出现在屏幕右侧随滚动进度一起滑翔**（专属空中飞行形态：后肢悬空收拢 + 扇翅与金色轨迹，不迈步踏地，贴右缘不挡内容）；滚到底部有像素**跑道**，此时猫飞向**屏幕中央放大 2.5~3 倍大特写亮相**并弹出自适应提示（PC 端"继续下滑，起飞进入游戏！"；移动端自适应为"继续上滑，起飞进入游戏！"且箭头指向上方）；此时用户继续向下滑动（PC 滚轮向下或移动端手指顺势持续向上划），猫顺势**帅气俯冲降落到左侧跑道起跑线**，并随着滑动在跑道上**向右全力狂奔冲刺**（滑得越多跑得越远，双脚快速交替踏步并扬起金色粒子碎屑；若停止滑动或反向回滑则平滑倒退回起点防误触）；冲刺到跑道右侧尽头时猫**纵身向上一跃（切换回展翅飞天姿态）**，以起跳点为圆心展开圆形扩散动画，丝滑进入全屏游戏层。**双游戏体系与切换**：
1. **飞行猫避障（默认进入）**：类 Flappy Bird 风格横版避障飞行，支持**键盘 ↑ / ↓ 或 W / S 控制上下**以及**鼠标移入平滑跟随**；具备**动态飞行仰角（向上爬升仰头、向下俯冲低头）与粒子喷流顺应**；障碍物为上下立柱（留缝隙）与移动翼龙（混合障碍），撞击即死；右上角实时计分与最高分（localStorage `mcryii-fly-hi`）；
2. **恐龙跑酷**：经典小恐龙玩法，空格/↑跳、↓蹲、Esc 退出；仙人掌与翼龙障碍，最高分存 localStorage `mcryii-dino-hi`；
3. **游戏切换器**：游戏层**左上角**设有胶囊切换按钮（`#dino-switch`），点击可在飞行猫与恐龙跑酷之间即时循环切换，底层由 `window.__gameManager` 驱动，后续可无缝注册接入更多新游戏；
4. **色彩实时跟随**：游戏右上角有**主题切换按钮**（月亮/太阳，与顶栏联动）；同时底层注册 `MutationObserver` 实时监听 `['data-theme', 'data-palette']`，无论是主页滑翔猫还是游戏内画面，切换 6 套调色板或明暗主题时**即刻全局变色，无需刷新网页**。

【改哪里】
- 欢迎语文字 → `hugo.yaml` 的 `params.homeInfoParams`
- 背景图 → 替换 `static/images/home-hero.jpg`（建议 1920 宽 JPG，同名替换即可）；背景层与遮罩 → `layouts/index.html` 的 `.home-hero-bg`（PJAX 离开主页时随 `<main>` 自动销毁）
- 欢迎语排版/位置/移动端 → `layouts/partials/extend_head.html` 的 `page-bg-home` 样式块（此页取消 `main.main` 最大宽度限制，文字定位于左下；h1 加了 `text-wrap: balance` 让窄屏换行两行均衡，别删）
- 打字机脚本 → `layouts/partials/extend_head.html`；首页滚动淡出、全局滚动帧调度与减少动态效果 → `static/js/scroll-effects.js`
- 飞行猫游戏逻辑 → `static/js/fly-game.js`（专属空中飞行像素姿态 `CAT_FLY_UP/DOWN`、爬升与俯冲动态倾角、移动速度、立柱缝隙高度、翼龙刷新率、流线型尾迹粒子、`setPal` 色彩响应都在此调整）
- 恐龙游戏逻辑/游戏管理器/共享像素数据/手势检测/进出动画 → `static/js/dino-game.js`（像素画、空中姿态 `CAT_FLY_A/B`、跑道冲刺迈步 `CAT_A/B`、触底容差 `atBottom`、移动端触控动力学映射 `touchDelta = touchLastY - curY`、难度、得分率、重力/跳跃常量、`window.__gameManager` 调度、全站主题与色板 `MutationObserver` 监听都在此文件；通过 `window.__dinoShared` 导出吉祥物像素与绘制函数供各游戏复用）
- 游戏挂载点（数据卡/云/跑道 + 飞行猫全屏画布 + 提示胶囊（`.hint-desktop` / `.hint-mobile`） + 游戏层 + 游戏切换按钮/主题/关闭按钮）→ `layouts/index.html`（含 `.home-descent` 与全屏 `#dino-fly` 样式）
- 提示胶囊（桌面/移动端响应式文案与向上箭头动画）/游戏层/切换按钮/关闭按钮样式与脚本引入 → `layouts/partials/extend_head.html` 的"主页彩蛋"样式块
- 飞行猫滑翔/特写/跑道冲刺/起跳逻辑在 `dino-game.js` 的"下滑航程"段（订阅 `MCRYII_ScrollHub` 单帧调度驱动）

【注意点】
- 首页不显示文章列表，文章通过顶栏「文章」入口（`/posts/`）、归档、搜索访问
- 手机竖屏背景定位 `62% center`（偏人物），换图后如需调整改 `layouts/index.html` 媒体查询
- 首页滚动效果使用全站单帧调度器；浏览器启用 `prefers-reduced-motion` 时全站 CSS 动画自动降级（`extend_head.html`顶部媒体查询），标签漂浮页等 JS 动画也会停用
- 彩蛋只在主页生效（挂载点在 `<main>` 内，PJAX 离开自动销毁；手势监听全局只注册一次，靠挂载点是否存在判断）；游戏打开时锁定 body 滚动，退出恢复；"提示一次"状态存内存，同一页面会话只提示一次，之后一次手势直接进游戏
- 主页内容可滚动（数据卡 + 跑道约 1.5 屏），触发手势必须先滚到页面底部再继续滑动；修改主页内容高度后无需改彩蛋代码（触发条件按"是否到底"动态判断，触底容差放宽至 15px 兼容移动端地址栏收缩引起的视口浮动）；建站天数按 RegularPages 最早日期计算，已过滤零值日期页面
- 触发机制由跑道助跑驱动：滚到底部猫在中央大特写，继续滑动触发俯冲降落起跑线并在跑道上向右冲刺；跑道落脚点基于 `.home-runway` 动态实时测量（`getBoundingClientRect`），脚底像素级紧贴跑道上表面；助跑量程微调至黄金手感 4800px（PC 滚轮单次限速 ≤90px，连续滚约 10~14 下即可冲刺完成；移动端手指顺势持续向上划推即可触发，同时精确阻止页面橡皮筋反弹；向下拉且蓄力归零时才允许自然回滚页面顶部），并包含触底惯性过滤；冲刺到尽头时猫顺势纵身向斜上方跃起滞空，伴随 0.85s `cubic-bezier(0.16, 1, 0.3, 1)` 超柔顺减速圆形展开动画丝滑切入小游戏；中途停止滑动（>1200ms）或反向滑动平缓衰减退回起点
- 游戏管理器支持通过 `window.__gameManager.register(id, gameObj)` 自由挂载新小游戏，只需提供标准接口（`name, start, stop, resize, setPal, pressAction, onKeyDown, onKeyUp, onPointerMove, onPointerLeave`）；
- 配色跟随不仅覆盖深浅模式（`data-theme`），还实时覆盖了全站 6 套主题色板（`data-palette`），通过 MutationObserver 监测属性变动即刻热更新，切色板无需刷新页面；
- 改动 `dino-game.js` 或 `fly-game.js` 后记得升 `hugo.yaml` 的 `assetsVersion`，避免浏览器缓存旧脚本导致新功能不生效

### 3. 顶栏导航

【功能】三段式顶栏：站点名/头像 + 金色胶囊磁吸导航 + 右侧按钮（主题切换 Alt+T、搜索 Ctrl+K、移动端菜单）。桌面端胶囊内置**磁吸标签游标（Magnetic Glider）**：鼠标悬停任意菜单时金色光晕滑块平滑吸附，离开后自动回弹至当前激活项，激活项常驻呼吸柔光；下拉菜单具备自胶囊底部展开的弹性生长动效与子项错峰淡入。移动端保持抽屉菜单，滚动时顶栏自动隐藏。

【改哪里】
- 菜单项（增删页面、改名字）与指示器结构 → `layouts/partials/header.html` 最上面的 `$navItems` 字典及 `.nav-indicator` 元素
- 交互逻辑（下拉、抽屉、快捷键、磁吸滑块吸附与回弹）→ `static/js/nav.js`
- 样式（滑块样式、呼吸动画、生长动效、深浅色适配）→ `layouts/partials/extend_head.html` 顶栏 CSS 块
- 顶栏滚动隐藏、返回顶部、阅读进度条的统一调度 → `static/js/scroll-effects.js`

【注意点】菜单是**硬编码字典**，不是 hugo.yaml 的 menu。当前菜单：主页 / 文章（文章列表、分类、标签、归档）/ 网站导航 / 动态（动态、更新日志）/ 记录（笔记本、相册、音乐、日历、规划）/ 关于（关于我、留言板、友链、赞助、下载），"写文章"已移除。
- `params.assetsVersion` 当前为 `20260908-2`；修改 `nav.js`、`global-nav.js` 或 `scroll-effects.js` 后需要同步更新
- 磁吸滑块由硬件加速驱动（`translate3d`），在 PJAX 页面切换（`window.__syncNavActive`）与窗口 resize 时自动重算并校准位置，不影响触屏与移动端
- 滚动和 resize 效果通过单个 `requestAnimationFrame` 调度，页面级监听会在 PJAX 切换前清理
- 抽屉/游戏层锁滚动**不用** `body overflow:hidden`（iOS Safari 无效），nav.js 与 dino-game.js 各有一套 `lockBodyScroll/unlockBodyScroll`（`position:fixed` + top 位移补偿，解锁时还原滚动位置）；改滚动锁时注意 **Esc 处理不能无条件 `closeDrawer()`**——抽屉没开就动锁会把恐龙游戏持有的锁误清掉（nav.js 已加 open 守卫）
- 桌面下拉菜单兼容触屏（iPad 横屏等 ≥1024px 触屏）：mouseenter 时记录打开时间，click 若在 600ms 内跟进（tap 的合成事件序列）则视为"想打开"保持展开，超过则维持原"点击关闭"切换逻辑；`mouseenter` 里的时间戳别删
- `.icon-btn` 视觉 32px，但触屏/窄屏（`hover:none` 或 ≤1023px）下用 `::after` 把热区扩到约 46px（`inset:-7px`），改按钮尺寸时别把这个媒体查询丢了

### 4. 全站搜索

【功能】Ctrl+K 或点搜索按钮唤起弹窗，首次打开搜索或首次输入关键词时才加载本地 Fuse.js 与精简索引，模糊搜索文章/笔记/页面/动态的标题/摘要/分类/标签（结果带类型徽标），展示前 10 条结果；未打开搜索的页面不请求搜索资源。

【改哪里】
- 搜索栏 HTML/样式/加载逻辑 → `layouts/partials/extend_head.html` 的"搜索栏"部分
- 搜索索引字段 → `layouts/index.json`（只输出标题、链接、200 字摘要、分类、标签、类型 type）
- Fuse.js 6.6.2 本地文件 → `static/js/fuse-6.6.2.min.js`
- 搜索按钮/快捷键触发预加载 → `static/js/nav.js` 的 `openSearch()`
- 搜索资源缓存版本 → `hugo.yaml` 的 `params.assetsVersion`

【注意点】搜索索引（`index.json`）是构建期生成的，新增文章/笔记/动态后需重新构建才可搜到；索引只含搜索需要的精简字段，分类和标签可直接作为搜索词。Fuse.js 与索引都按需加载，加载失败时搜索框显示失败提示，再次打开/输入会自动重试，不影响页面其他功能。

### 5. 音乐播放器 + 音乐页

【功能】右下角悬浮播放器：支持**多歌单**（歌单面板顶部可切换歌单，当前歌单记忆在 localStorage `hugoPlayerPlaylist`）、顺序/单曲循环/随机、音量/静音、进度条拖动、歌单面板、折叠成小方框；PJAX 切换页面不断播、播放位置自动记忆。**播放源优先走 jsDelivr CDN（`https://cdn.jsdelivr.net/gh/MCRYII/MCRYII.github.io@master/static/music/...`，加速国内访问），加载失败自动回退本地 `/music/` 文件**。音乐页 `/music/` 为**全屏沉浸式舞台**（无侧栏）：动态极光背景 + 金色粒子、旋转黑胶唱片、Web Audio 实时频谱可视化、歌词面板（`.lrc` 同步滚动高亮，**默认隐藏，点唱片封面进入歌词聚焦模式（封面淡出隐藏），点歌词区域或按 L 键返回封面**；无歌词显示"纯音乐"）、金色发光控制区、歌单侧滑抽屉（抽屉内顶部可切换歌单）、快捷键（空格播放/暂停、←/→ 快退快进、↑/↓ 音量、N/P 切歌、M 静音、L 歌词、Esc 关歌单），与播放器实时联动。播放器与音乐页自动读取 mp3 内嵌专辑封面，无内嵌封面时用默认图。**音乐页完整跟随站点主题：顶栏太阳/月亮按钮即时切换深浅色（暗色=原暗金舞台，浅色=暖白纸感背景），左下角调色板 6 套配色实时跟随（极光/频谱/粒子/唱片光晕/进度条/控制区全部换色），均无需刷新**。

【改哪里】
- 加歌 → 把 mp3 丢进 `static/music/`，文件名格式 `作者 - 歌名.mp3`（自动拆分作者/歌名）
- 歌单 → `data/music.json`（playlists 数组：name + order 文件列表；"全部"歌单自动包含所有 mp3，order 可由写作工具拖动排序并自动同步，其他歌单手动引用；构建期合并目录与歌单生成播放器数据）
- 加歌词 → 同目录放同名 `.lrc` 文件（如 `作者 - 歌名.lrc`），页面自动按时间轴滚动高亮；没有 `.lrc` 的歌曲显示"纯音乐"；亦可在本地写作工具网页版的音乐管理页点击每首歌右侧的「词」按钮直接上传/替换同名 `.lrc` 文件
- 播放器 HTML/样式/逻辑 → `layouts/partials/extend_head.html` 中部（约 798–1645 行，「搜索栏」区块之前）
- 音乐页（全屏舞台样式 + 频谱/歌词/歌单抽屉/快捷键全部在此）→ `layouts/_default/music.html`
- 播放器和音乐页封面图片尺寸/解码属性 → `layouts/partials/extend_head.html`、`layouts/_default/music.html`

【注意点】
- CDN 直链依赖 GitHub 仓库里已提交的 `static/music/` 文件（jsDelivr 单文件限 20MB，当前所有音频均未超限）；CDN 失效时播放器自动回退本地文件，不影响播放
- 歌词 `.lrc` 始终从本地 `/music/` 读取
- 歌单数据是构建期生成的（`extend_head.html` 里 `const playlists`），加歌/改歌单后需重新构建；歌词 `.lrc` 是静态文件直接读取，加歌词后本地预览无需重建
- 本地预览时音乐页右上角有"管理音乐"按钮（`blogtool://edit?tab=music`，跳写作工具"音乐"页签：上传/删除/排序/歌单管理，线上隐藏）
- 播放状态存 localStorage（`hugoPlayerState` 位置/播放中、`hugoPlayerMode` 模式）
- 封面是运行时从 mp3 内嵌 ID3 标签提取的（`extend_head.html` 播放器脚本的 `extractCover` 函数），无需构建；首次播放某首歌时封面会先显示默认图、随后自动替换（每首歌只解析一次，内存缓存）
- 音乐页隐藏页脚、整页无白边；进入音乐页时通过 `body.music-page` 类临时隐藏右下角悬浮播放器，PJAX 切走自动恢复
- 音乐页**完整跟随站点主题**：深浅色由 `html[data-theme]` 驱动（暗色=原暗金舞台，浅色=暖白纸感背景+深色文字+站点默认顶栏，暗色玻璃顶栏覆盖只在非浅色主题生效）；强调色由 `--accent` / `--accent-rgb` 令牌派生，写死金色的样式一律用 `rgba(var(--accent-rgb), a)` / `var(--accent)` / 局部令牌 `--ms-*`（暗色默认值在 `body.music-page`，浅色覆盖在 `html[data-theme="light"] body.music-page`）；画布颜色（频谱 16 档 + 粒子）由 JS `syncThemeColors()` 从 computed style 读取生成，rAF 循环里比对 `data-palette|data-theme` 字符串、变化才重建（调色板/主题切换无事件广播，别改成事件监听以外的方案时漏掉这个轮询）；浅色下粒子透明度自动 ×0.45；黑胶盘体保持黑色不随主题
- 频谱可视化基于 Web Audio API，受浏览器自动播放策略限制，需页面内点击/键盘操作一次后启动（未启动时自动降级为静态动画条）
- 快捷键处理（`msKeyHandler`）**不劫持**：Ctrl/Alt/Meta 组合键、输入框/下拉/可编辑元素、以及焦点在按钮/链接上的空格/回车/方向键（留给浏览器激活按钮与移动焦点），改这段前先想清楚键盘用户路径
- **音乐页性能约定**（改动样式/动画前先看这条，别把开销加回去）：极光背景不用 `filter: blur`（靠 radial-gradient 自身软边过渡，模糊滤镜在 52vmax 大元素上是最大 GPU 杀手）；音乐页顶栏胶囊**不加** `backdrop-filter`（暗色实底下毛玻璃看不出且每帧重采样）；rAF 主循环播放时满帧、暂停时降到约 15fps；粒子 canvas 固定 1x DPR、数量上限 16；频谱 canvas 的 2d context 全局缓存，不要每帧 `getContext`
- 音乐页封面已显式声明尺寸，避免播放器与黑胶封面加载时产生布局跳动
- 播放器进度条/音量条的金色"已播放/当前音量"填充由 CSS 变量 `--fill` + 脚本 `updateSliderFill()`（loadedmetadata / timeupdate / input 事件同步）驱动；音量控件（静音按钮 + 滑块）位于标题行 `[ 音乐 ]` 右侧，与折叠按钮同行
- **seek-bar 禁止加 `class="progress-bar"`**：该类名属于顶部阅读进度条（带 `transform: scaleX(0)` 和 `background: var(--gold) !important`），加上后播放器进度条会被按阅读百分比压成 0 宽度，表现为"进度条消失"（2026-09 踩坑后已移除并留注释防回归）

### 6. 侧边栏（三栏布局）

【功能】全站三栏布局：
- 通用页左侧：博主信息卡（头像/文章数/分类数/标签数/社交链接）、音乐卡、站点统计（文章/分类/标签/总字数/运行天数/最后更新）。统计口径：文章数只数 `posts` 栏目；运行天数按最早文章日期动态计算（与首页「建站航程」同源，别再写死日期）；总字数 = `posts` + `notes` 的 WordCount 之和
- 通用页右侧：日历卡、浏览量卡（不蒜子 UV/PV）、今日一言、最近更新 5 篇
- 文章页左侧：全部分类折叠树、"写新文章"入口、音乐卡
- 文章页右侧：目录、最近更新

【改哪里】
- 通用页 → `layouts/partials/sidebar-left.html`、`layouts/partials/sidebar-right.html`
- 文章页 → `layouts/partials/posts-sidebar-left.html`、`layouts/partials/posts-sidebar-right.html`
- 头像与侧边音乐封面尺寸 → `layouts/partials/sidebar-left.html`、`layouts/partials/posts-sidebar-left.html`；头像图片路径 → `static/images/mcryii-avatar.jpg`

【注意点】音乐卡通过 `window.__mcryiiPlayer` API + `mcryii:player` 事件与播放器同步，改代码时别改这两个接口名。
- 头像和侧边音乐封面保留固定尺寸与异步解码属性，避免侧栏内容加载后跳动；头像使用带有微留白的安全正方形图，适配 50% 圆形裁剪遮罩不切角。

### 7. 文章系统（列表页 / 详情页 / 封面）

【功能】
- 列表页 `/posts/`：首屏"最新发布"大图 + 其余文章竖排列表（滚动渐入动画）+ 分页；左侧栏注入完整博主名片（Profile Card）
- 详情页：标题日期居中、金色下划线、标签、上下篇导航、相关文章（按标签/分类计分取前 3 张封面卡片）、评论；目录显示在右侧侧栏；左侧栏带有博主名片与分类折叠树；**单页内不额外渲染封面图**，直接展示正文内容与插图，杜绝同一张图重复出现
- 封面系统：文章通过 Front Matter 的 `cover.image` 指定封面图，主要用于文章列表卡片、归档页、分类/标签页及相关文章推荐作为卡片缩略图；无封面自动回退默认图
- Markdown 正文排版：行高升级为 1.85 黄金阅读比例，段落舒展（margin-bottom 1.4rem），标题上下留白优化；所有正文插图统一 `width: 100%; max-width: 100% !important; height: auto !important;` 舒展满宽自适应展示，杜绝溢出与比例畸变；构建期自动输出懒加载、异步解码属性
- 文章列表首屏封面和竖排文章封面显式声明比例，降低图片加载造成的布局偏移
- 卡片标签复用全局金色变量，多层弥散柔和阴影，hover 浮起微光更具质感

【改哪里】
- 文章内容 → `content/posts/*.md`（Front Matter：title、date、categories、tags、draft、cover.image）
- 文章图片 → `static/images/`
- 列表页版式 → `layouts/posts/list.html`
- 详情页版式 → `layouts/posts/single.html`
- 文章区左侧栏（博主名片 + 分类折叠）→ `layouts/partials/posts-sidebar-left.html`
- 相关文章卡片 → `layouts/partials/related-posts.html`（`posts/single.html` 引用）
- 列表/分类/标签页封面缩略图 → `layouts/partials/cover-thumb.html`（构建期由 `scripts/gen_thumbs.py` 生成 `static/images/thumbs/`，640px JPG，缺失时回退原图）
- Markdown 图片渲染属性 → `layouts/_default/_markup/render-image.html`
- 正文 1.85 行高排版、图片自适应与设计令牌 → `layouts/partials/extend_head.html`
- PJAX 页面级效果清理与图片放大范围 → `static/js/global-nav.js`
- 全站滚动调度与阅读进度条 → `static/js/scroll-effects.js`、`layouts/partials/extend_head.html`

【注意点】`cover.image` 建议 5:3 或 16:9，仅用于外层列表和卡片推荐，文章单页内不渲染封面大图，直接展示正文，避免与正文首图出现两张重复插图；本地预览时文章页头部有"在写作工具中编辑"按钮（`blogtool://` 协议，线上不显示）。全站 `static/images/` 图片已统一压缩优化（PNG 无损/量化取优、JPG quality 85，尺寸与透明度不变），覆盖文章封面、动态、笔记、相册图片；新增图片建议先压缩再放入，避免体积回涨。图片放大脚本（medium-zoom）仅针对正文插图生效，排除非正文区域。正文图片的外链只补懒加载/解码属性，不读取本地尺寸；站内 `/images/` 图片文件存在时才输出宽高。
正文内容排版（1.85 黄金行高、舒展段落间距、链接金色下划线、引用块金色左边线与卡片底色、行内代码/代码块、表格、图片圆角、文字选中色）统一定义在 `extend_head.html` 的「正文内容排版细节」样式块；文章页元信息（中文日期/阅读时长/字数）由本地覆盖的 `layouts/partials/post_meta.html` 输出。
缩略图在本地预览（`打开博客.ps1`）和 GitHub Actions 构建时自动生成，新增图片后重新构建即可；缩略图统一转 JPG，透明 PNG 会失去透明背景（封面场景无影响）。

### 8. 分类 / 标签

【功能】`/categories/` 卡片式分类页（每类显示图标、篇数、前 4 张缩略图、最近文章）；`/tags/` 标签漂浮页：全部标签随机散布在金色星空舞台上漂浮（大小随文章数缩放，hover 发光），点击标签后平滑滚动到下方结果区，以卡片列表（标题/日期/封面）展示该标签下的文章，支持关闭结果区返回。文章自动按 Front Matter 归类。

【改哪里】
- 分类卡片样式 → `layouts/_default/terms.html` 的 category 分支
- 标签漂浮页（页面结构/漂浮动画/点击交互）→ 同文件 `terms.html` 的 tags 分支（内联 style + script，标签与文章数据由构建期生成注入）
- 顶栏「标签」入口 → `layouts/partials/header.html` 的「文章」下拉（已添加）
- 分类/标签数据 → 直接在文章 Front Matter 里写，不用单独建文件

【注意点】由 Hugo taxonomy 自动生成，改分类名只需改文章 Front Matter。标签页的文章数据是构建期生成（页面内联 `jsonify` 数据），新增文章后需重新构建；漂浮动画只用 CSS transform/opacity（GPU 合成），PJAX 进入页面时脚本随 `<main>` 重新执行，页面级 resize 监听已注册 `MCRYII_PageUnsubscribers` 换页清理；系统开启「减少动态效果」时自动停用漂浮动画。

### 9. 归档（/archive/）

【功能】按年 → 月 → 日期列出全部文章。

【改哪里】`layouts/_default/archive.html`（样式 + 结构）；数据自动来自 `content/posts/`；月份按"N 月"中文显示（模板内 `GroupByDate "1"`）。

### 10. 日历（/calendar/）

【功能】三栏布局：中间大月历 + 右侧功能栏（节日·纪念日、当天日程、即将到来、添加日程）。文章日金色标记、节日角标（SVG 图标，继承文字色）、右侧节日列表红字、个人日程蓝点、今天高亮；点日期看当天文章/节日/日程；支持添加/删除日程、到点提醒（页面顶部提示条 + 浏览器通知，需授权）。通用页侧边栏仍保留一个小日历。

【改哪里】
- 页面结构/样式/逻辑 → `layouts/_default/calendar.html`
- 节假日/纪念日数据 → `data/events.json`（events 数组：date 用 `YYYY-MM-DD` 固定日期或 `MM-DD` 每年循环；type 用 festival=节日 / memorial=纪念日；icon 是日历角标对应的 SVG 图标名）
- 侧边栏小日历 → `layouts/partials/sidebar-right.html`
- 农历节日自动生成 → `scripts/gen_lunar_events.py`（`python scripts/gen_lunar_events.py <年份>` 自动计算并更新指定年份农历节日）
- 纯逻辑自检 → `scripts/test_calendar_logic.js`（`node scripts/test_calendar_logic.js`）

【注意点】
- 日历文章数据（`__calBigData`）与节日数据（`__calEvents`）构建期由 Hugo 生成，新增文章后重新构建
- 个人日程存 localStorage（键 `mcryii_calendar_schedules`），换浏览器/清缓存会丢；提醒只在打开日历页时检查（静态站无后端推送，不打开页面就不会弹）
- 农历节日（春节/端午/中秋等）公历日期记录在 `data/events.json`，每年年初运行 `python scripts/gen_lunar_events.py <年份>` 即可自动计算并写入，无需手动查日历逐条录入

### 11. 动态（/dynamic/）

【功能】公告板：便签动态（文字 + 最多 4 张图片 + 链接）与文章剪报按时间合并排列；便签是贴纸风格（旋转角度、图钉装饰），列表图片使用 640px JPG 缩略图，点击才加载原图并灯箱预览（支持 ← → 翻页、ESC 关闭）。

【改哪里】
- 动态数据 → `data/moments.json`（moments 数组：id、date、text、images、link）
- 动态图片 → `static/images/moments/`
- 动态列表缩略图 → `scripts/gen_thumbs.py` 递归生成 `static/images/thumbs/moments/`，模板通过 `layouts/partials/cover-thumb.html` 引用
- 页面结构/样式/灯箱 → `layouts/_default/dynamic.html`
- PJAX 换页清理 → `static/js/global-nav.js`（移除动态页残留的灯箱层）

【注意点】动态和文章按时间合并排序；本地预览时显示"发动态 / 编辑"按钮（`blogtool://` 协议，线上隐藏，编辑链接按动态 `id` 定位）；建议用写作工具的"动态"页管理，会自动同步写入 `data/moments.json`（旧数据无 `id` 的会在读取时自动补齐）。新增动态图片后需重新运行缩略图生成或重新构建，列表用缩略图、灯箱自动回到原图。灯箱层挂在页面级，PJAX 切回其他页面时会由 `global-nav.js` 清理；以后新增页面级弹层也要在换页时清理，否则会残留到新页面。

### 12. 笔记本（/notes/）

【功能】学习笔记（当前 13 篇，分 **51单片机（8 篇）+ ESP32（5 篇）** 两组）：列表按编号展示，支持分类筛选 chips、分组折叠；详情页带编辑器风格代码块。

【改哪里】
- 笔记内容 → `content/notes/*.md`（Front Matter 需 `num` 编号、`group` 分组、`description` 简介、date）
- 列表页 → `layouts/notes/list.html`
- 详情页 → `layouts/notes/single.html`
- 正文小标题图标 → `layouts/shortcodes/icon.html` + `layouts/partials/header.html` 的 SVG sprite

【注意点】列表按 `Params.num` 排序，`group` 决定分组。详情页 meta 栏（№ 编号、日期、阅读约 X 分钟、分组标签）中的分组标签读取 front matter `group`（此前写死成 ESP32，51单片机笔记会显示错误分组，已修复）。

### 13. 网站导航（/site-nav/）

【功能】个人书签管理页：`static/bookmarks.json`（当前 114 条、7 个分类，页面 fetch `/bookmarks.json` 直接读这份）渲染书签卡片（自动抓取网站 favicon），支持实时搜索；"ACG"、"呃呃"两个分类可隐藏（另有 URL 含 dl666123 关键字的链接一律隐藏），输入密码可解锁查看；公开书签里已不含隐藏分类，线上不显示解锁按钮，解锁只在本地合并 bookmarks-local.json 后才有实际内容。本地预览时还会合并加载**本地专属书签** `static/bookmarks-local.json`（成人内容等，仅本地可见，线上不出现）。

【改哪里】
- 公开书签 → `static/bookmarks.json`（categories 数组：name + links，每链接 title/url；此前的 `data/bookmarks.json` 重复副本已删除，别再建）
- 从浏览器书签 HTML 重新导入 → `scripts/convert_bookmarks.py`（只输出 static/bookmarks.json 一份）
- 本地专属书签 → `static/bookmarks-local.json`（已加入 `.gitignore`，不会进公开仓库；格式与 bookmarks.json 相同，本地预览时自动合并）
- 隐藏分类列表、解锁密码 → `layouts/_default/site-nav.html`（`HIDDEN_CATS` 数组、`LOCK_HASH` 是密码的 SHA-256 哈希）
- 书签 favicon 显式尺寸与异步解码 → `layouts/_default/site-nav.html`

【注意点】密码校验在前端 JS 里，防君子不防小人；解锁状态存 localStorage（`site-nav-unlock-v1`），换浏览器会重置。成人内容已移入本地文件，注意不要把它提交到 GitHub。
- favicon 卡片图片声明 64×64 尺寸，避免异步加载时书签卡片内容跳动

### 14. 友链（/friends/）

【功能】友链卡片网格（头像 + 名字 + 简介），当前为空，显示占位提示。

【改哪里】`data/friends.json`（friends 数组：name、url、avatar、desc）。

### 15. 更新日志（/changelog/）

【功能】时间线展示站点更新记录。

【改哪里】`data/changelog.json`（entries 数组：date、title、items 列表）。

【注意点】每次推送线上更新前，必须在 `data/changelog.json` 的 `entries` 顶部同步追加当次更新记录条目，确保站内更新日志与 Git 提交版本对齐。


### 16. 规划（/planning/）

【功能】年度目标看板：目标卡片按"进行中/待开始/已完成"分组，显示进度条、优先级、截止日期；待开始目标点"开始"按钮会本地切到进行中。

【改哪里】`data/planning.json`（goals 数组：title、status、progress、deadline、priority、note）；页面样式在 `layouts/_default/planning.html`。

【注意点】"开始"状态存 localStorage（`plan-started-v1`），是本地记忆，换浏览器会重置，不会同步到线上数据。

### 17. 下载页（/downloads/）

【功能】自动扫描 `static/files/` 目录，按文件夹分组，以金色卡片网格展示文件（按扩展名自动匹配图标 + 可读大小），分类可折叠（默认展开，状态本地记忆）。每张卡片有两个入口：主按钮默认走 jsDelivr CDN 加速（`https://cdn.jsdelivr.net/gh/MCRYII/MCRYII.github.io@master/static/files/...`），也可在 `data/downloads.json` 为单个文件配置网盘/对象存储直链（配置后主按钮显示"网盘"）；"本地"按钮始终指向本站 `/files/` 直链作为备用。

【改哪里】把文件放进 `static/files/<分类目录>/` 即可自动列出；页面样式在 `layouts/_default/downloads.html`。

【注意点】构建期用 `readDir` 扫描生成，加文件后重新构建；折叠状态存 localStorage（`download-collapse-v1`），换浏览器会重置。外链配置在 `data/downloads.json` 的 `files` 字典（键为"分类/文件名"，值为直链 URL），可在写作工具下载页用"外链"按钮配置，留空清除。本地预览时页面顶部有"管理下载文件"按钮（`blogtool://edit?tab=downloads` 协议，跳转写作工具下载管理页签，线上隐藏）。卡片图标是模板里以字符串变量拼出的 SVG，输出时必须加 `| safeHTML`，否则会被 Hugo 转义成可见代码文本；页面图标尺寸依赖 `extend_head.html` 里的全局 `.site-icon` 样式，如果整站图标变成 300×150 巨图，先重启 Hugo 开发服务器（旧进程可能输出过期渲染）。

### 18. 留言板（/guestbook/）+ 评论系统

【功能】全站文章和留言板集成 Cusdis 评论（无后端、免费），评论区高度自动适配；评论区**完整跟随站点深浅色与调色板**（iframe 内文字/输入框/按钮/链接按站点令牌色注入，深浅色或色板切换时实时重注入，无白底突兀）。

【改哪里】
- 评论组件 + 主题同步脚本 → `layouts/partials/comments.html`（Cusdis app-id 与 iframe 样式注入逻辑都在这里）
- 单篇文章关闭评论 → 该文章 Front Matter 加 `comments: false`
- 留言板正文 → `content/guestbook.md`

【注意点】PJAX 切换页面时 Cusdis 有单独初始化逻辑（`static/js/global-nav.js` 的 `initCusdis()`，`executeScripts` 会跳过 cusdis 脚本防止重复执行），别和普通脚本混在一起。评论 iframe 是 **srcdoc 同源文档**，内部颜色写死浅色（Tailwind `dark:` 类只跟系统偏好走、不跟站点主题），所以 comments.html 里用脚本按 `data-theme`/`data-palette` 往 iframe 注入 `<style id="mcryii-cusdis-theme">`；注入的 `color-scheme` 规则**必须带 `!important`**——Cusdis 自带的 `:root{color-scheme:light}` 排在后面会把同权重规则盖掉，浅色 scheme 下 Chrome 会把透明 iframe 画布涂成白色；Cusdis 重渲染会重建 iframe（样式被清掉），已有 childList 观察器自动补注入，清理已注册 `MCRYII_PageUnsubscribers`。

### 19. 赞助（/sponsor/）

【功能】赞助说明 + 收款码占位框。

【改哪里】正文在 `content/sponsor.md`；收款码占位 → `layouts/_default/sponsor.html` 的 `.qr-placeholder`（目前是占位文字，换图片即可）。

### 20. 其他单页（关于 / 相册）

【功能】三栏布局的 Markdown 内容页。

【改哪里】
- 关于我 → `content/about.md`
- 相册 → `content/gallery.md`（图片放 `static/images/`）

【注意点】"写文章"页面已删除，写作入口改为写作工具（桌面版 / 网页版）+ 本地预览时的 `blogtool://` 编辑链接。

### 21. 页脚（一言 + 站点统计）

【功能】页脚显示一言每日语录（调用 hitokoto API，localStorage 按天缓存）+ 不蒜子访客/阅读统计。

【改哪里】`layouts/partials/extend_footer.html`（HTML + 脚本 + 样式）；样式变量可配合 `static/css/custom.css`。

【注意点】一言接口挂了有兜底文案，不影响页面。

### 22. 统计与分析（Cloudflare）

【功能】Cloudflare Web Analytics 全站埋点。

【改哪里】`layouts/partials/extend_head.html` 最顶部（`data-cf-beacon` 里的 token）。

### 23. 本地写作工具（blog_tool.py）

【功能】写作助手，两种形态：
- **桌面版**：`python blog_tool.py`（Tkinter 窗口）——文章列表管理、新建/编辑/删除文章、5:3 封面裁剪编辑器、Markdown 预览、一键插入图片、启动本地预览、一键 git 提交推送（有确认弹窗）
- **网页版**：`python blog_tool.py --web`，浏览器打开 http://127.0.0.1:8777/（仅本机可访问），支持 URL 参数直达页面（如 `?tab=ai`、`?tab=moments`、`?tab=downloads`、`?tab=music`）；界面图标为内嵌 SVG 线条图标（金色描边，无 emoji）；文章列表可收起（列表顶部"收起"，编辑区左上角"展开列表"）；文章编辑支持 Markdown 预览（图片缩到 360px 显示，悬停右上角删除图标可从正文删除该图片行，服务端提供 `/images/`、`/files/` 静态映射让本地图片可显示，外链加载失败会显示红色提示；切换文章自动清空预览）、分类检索（输入时下拉已有分类可直接选）、标签 chip 输入（回车添加、删除图标）；**AI 图库页 + 统一图片来源弹窗**：插入图片 / 上传封面 / 动态添加图片时可选择本地上传、AI 图库或一句话调用 ComfyUI 生成
- **动态管理**：内置"动态"页，支持发布、编辑（改文字/增删图片/改链接，发布时间保持不变）、删除；读写 `data/moments.json`，图片自动复制到 `static/images/moments/`，编辑或删除时清理不再引用的图片文件
- **下载管理**：桌面版"下载"标签页 + 网页版"下载"页，支持上传文件到 `static/files/<分类>/`、新建分类、**重命名分类**、删除文件、给文件**配置外链**（存 `data/downloads.json`，下载页主按钮优先走外链）；上传是复制到目标目录，重新构建后下载页自动更新
- **音乐管理**：网页版"音乐"页（歌单列表 + 当前歌单歌曲），支持上传 mp3（文件名 `作者 - 歌名.mp3`）、上传/替换同名 `.lrc` 歌词（歌曲带「词」标记，绿色表示已有歌词、灰色表示无歌词，点击一键上传）、删除文件、歌单内上移/下移 + 鼠标拖拽排序、新建/删除歌单、把"全部"里的歌加入其他歌单、从歌单移出；数据写 `data/music.json`，重新构建后播放器/音乐页生效

【配套文件】
- `config.local.json` —— 本地路径与端口配置（已 `.gitignore`，支持自定义 `blog_root`、`comfy_root`、`comfy_python`、`comfy_port`，换机器直接改此文件）
- `start_blog_tool.bat` —— 启动器（可用环境变量 `BLOG_TOOL_PY` 指定 Python 路径）
- `install_blogtool_protocol.bat` —— 注册 `blogtool://` 协议（运行一次即可，让站点上的编辑按钮能唤起工具）
- `打开博客.bat` / `打开博客.ps1` —— Windows 端一键启动 Hugo 本地预览并打开浏览器；现已全面升级为桥接 WSL2：每次运行会检测 WSL 内的 Hugo 状态并无窗口唤起 `/home/mcryii/myblog-new/run.sh --daemon`（自动比对 JS 递增 `assetsVersion`、生成缩略图、检查索引），服务就绪后自动唤起 Windows 浏览器访问 `http://localhost:1313/`，桌面快捷方式（如 `MCRYII.lnk`）零配置无缝使用。
- `blog_tool_icon.ico` —— 工具图标

【改哪里】
- 本地机器路径与端口配置 → `config.local.json`
- 功能入口、ComfyUI 接口和后台启动逻辑 → `blog_tool.py`
- AI 导入压缩与启动命令测试 → `scripts/test_ai_workflow.py`
- 使用说明 → `指南.md`

【注意点】机器路径优先从 `config.local.json` 读取（`blog_root`、`comfy_root`、`comfy_python`、`comfy_port`），换机器无需修改 `.py` 源码，未提供时回退默认硬编码路径；推送前有确认对话框，符合"本地优先、禁自动推送"；`blogtool://` 编辑按钮只在本地预览（localhost）时显示；下载管理操作的是 `static/files/` 下的真实文件，删除不可恢复（有确认弹窗）；网页版上传走 base64，**上限 20MB**（前端/后端双重校验，超限会提示改用桌面版）；分类名仅限字母/数字/-/_；"全部"歌单的 order 会自动与 `static/music/` 实际文件同步，可拖动排序；上下箭头与拖拽排序都即时写入 `data/music.json`。

### 24. 本地图片转文字代理（image-proxy）

【功能】Codex 发送图片时，自动把 `image_url` / `input_image` 交给阿里云百炼 Qwen 视觉模型（`qwen3.5-omni-plus-2026-03-15`）转成文字描述，再转发给 CC Switch 的 DeepSeek 链路，避免 DeepSeek 不识别图片导致 400。用户仍可直接粘贴图片，不需要手动运行 vision skill 或切换模型。

【改哪里】`image-proxy/proxy.js`、`image-proxy/start-image-proxy.cmd`、`image-proxy/test-proxy.js`；Codex 配置 `C:\Users\MCRYII\.codex\config.toml` 的 `base_url` 已指向 `http://127.0.0.1:15722/v1`；桌面快捷方式 `C:\Users\MCRYII\Desktop\启动图片代理.lnk`。

【注意点】代理监听 `127.0.0.1:15722`，CC Switch 继续监听 `15721`；代理必须保持运行，重启电脑后运行 `image-proxy/start-image-proxy.cmd` 即可恢复。CC Switch 切换 provider 可能重写 `config.toml`，同样运行该脚本会重新指回代理；修改 `proxy.js` 里的模型名后必须重启代理才会生效；图片转文字按 Qwen 视觉 API 计费。

### 25. CI/CD 部署

【功能】推送 `master` 分支后，GitHub Actions 自动构建（先运行 `scripts/gen_thumbs.py` 生成缩略图，再 `hugo --minify`）并部署到 GitHub Pages。

【改哪里】`.github/workflows/hugo.yml`（Hugo 版本 0.162.1、构建/部署步骤）。

【注意点】`public/` 已 gitignore；部署是自动的，任何推送前需用户明确批准。

### 26. 主题（PaperMod）

【功能】基础主题，提供默认布局和组件。

【改哪里】`themes/PaperMod/`（git submodule，一般不动）；本站 `layouts/` 下的同名文件会覆盖主题文件。

【注意点】更新主题 = 更新 submodule，可能影响本站覆盖的样式。本站已本地覆盖 `post_meta.html`（中文日期"2006-01-02"+ 阅读时长 + 字数，替代主题默认英文格式）与 `footer.html`（中文页脚署名"由 Hugo 驱动 · 主题 PaperMod"，其余脚本与主题保持一致），升级主题后若这两个文件有变需手动同步。

### 27. AI 图片工作流（ComfyUI）

【功能】写作工具网页版新增"AI 图库"页和统一"图片来源"弹窗：
- AI 图库：列出 `D:\Downloads\Programs\ComfyUI-aki-v3\ComfyUI\output` 最近 100 张图，可设为封面 / 插入正文 / 发动态 / 单张删除 / 多选批量删除；点击图片可放大，并显示 ComfyUI 写入的提示词与生成参数。
- 图片来源弹窗：文章插入图片、上传封面、动态添加图片时可选本地上传、AI 图库、AI 生成。
- ComfyUI 管理：网页版可检测运行状态、无窗口启动、一键结束；生成时用内置标准 txt2img 工作流（模型下拉从 ComfyUI 读取），生成进度在工具内实时显示。
- 导入优化：透明 PNG 保留 PNG 并优化，无透明转 JPG quality 85；不缩放尺寸，不修改 ComfyUI 源文件。

【改哪里】
- `blog_tool.py`：新增 ComfyUI 配置、启动/状态/图库/生成/导入接口、AI 图库页、图片来源弹窗
- `scripts/test_ai_workflow.py`：AI 导入压缩与启动命令测试

【注意点】
- 路径固定：`COMFY_ROOT`、`COMFY_PYTHON`、`COMFY_PORT=8188` 硬编码，换机器要改。
- ComfyUI 未运行时"AI 图库"仍可用（直接读 output 目录），但生成和模型列表不可用。
- 后台启动无窗口；由写作工具启动的 ComfyUI 不会随写作工具退出而自动关闭。
- 删除会直接删除 ComfyUI `output` 里的真实文件，不可恢复；ComfyUI 自己的历史记录可能仍保留失效缩略图，不影响博客。
- “结束”会终止 ComfyUI 及当前生成任务；如果是外部启动的 ComfyUI，会先确认占用 `8188` 的进程属于 `ComfyUI-aki-v3` 再结束。
- 大图参数从 PNG 内嵌元数据读取，只有 ComfyUI 带元数据保存的图片才能显示完整提示词/参数。
- 第一版生成工作流只覆盖标准 SD/SDXL checkpoint；Flux 或依赖自定义节点的工作流需要后续扩展。

### 28. 404 页面

【功能】自定义 404 页：大号 404 字样、「搜索内容」按钮（打开全站搜索弹窗并预载搜索索引）、随机推荐 3 篇文章卡片（“随便看看”，基于 Hugo 的 `shuffle` 实现）。

【改哪里】`layouts/404.html`（自包含模板：自带样式与脚本，复用全站搜索的 `window.MCRYII_LoadSearch` 加载逻辑）。

【注意点】GitHub Pages 部署后自动生效；本地预览时直接访问一个不存在的路径即可查看。

### 29. 调色板浮窗（主题配色）

【功能】左下角常驻调色板按钮（与右下角音乐浮窗对称），点开面板内置 6 套主题配色（金色·默认 / 樱粉 / 湖青 / 天蓝 / 暮紫 / 暖橙），点击即时切换全站强调色（顶栏 hover、链接、胶囊标签、按钮、日历、悬浮播放器等全部跟随），每套配色自动适配明暗两档主题（暗色下强调色提亮一档）；选择存 localStorage（`mcryii-palette`），刷新/换页保持，Esc 或点击面板外收起。**支持通过 URL hash 传递与分享主题配色（如 `#palette=sakura`），初次访问带 hash 时优先展示对应色板；切换配色时自动通过 `history.replaceState` 同步更新 URL hash，直接复制浏览器地址栏即可分享特定配色给他人**。

【改哪里】
- 色板定义 → `layouts/partials/extend_head.html`「主题配色板」样式块（每套配色一个 `html[data-palette="xxx"]` 选择器 + `[data-theme="dark"]` 暗色覆盖，改色值只动这里）
- 浮窗 DOM/CSS/交互脚本 → 同文件「主题配色板」区块（挂在 head，脚本只执行一次，无 PJAX 泄漏）
- 调色板图标 → `layouts/partials/header.html` SVG sprite 的 `ic-palette`

【注意点】全站强调色已令牌化：`:root` 的 `--accent` / `--accent-rgb` / `--accent-deep` / `--accent-contrast` 是唯一色源（`--gold` 是 `--accent` 的别名），**新写样式禁止再写死 `#d4af37` 或 `rgba(212,175,55,x)`**，一律用 `var(--accent)` / `rgba(var(--accent-rgb), x)`，否则换色板时该处不变色。顶栏整套配色（胶囊底色、导航文字、下拉/抽屉底色、站名色）也由色板经 `color-mix` 从强调色自动派生（定义在 `:root` 与暗色覆盖的 `--nav-*` / `--site-header-text` / `--drawer-*` 变量里），新增色板无需单独适配顶栏。`/music/` 全屏舞台刻意保持金色特型（模板字面量未令牌化）。头部有一段防闪脚本，抢先从 localStorage 恢复 `data-palette`，避免刷新时闪回金色；页面加载时会检查 `location.hash` 中的 `#palette=xxx` 参数，优先级高于 localStorage，便于带主题分享链接。

### 30. B 站视频文章同步（sync_bili_posts.py）

【功能】自动从 B 站个人主页（默认 UID: 678345234）拉取最新投稿视频，生成符合博客规范的 Markdown 文章并下载封面图：
- **增量同步**：默认比对本地已收录文章（`bili-*.md`）的最新发布时间，仅拉取并生成在此之后的新视频文章；传入 `--all` 参数可全量处理历史上未建档的旧视频。
- **文章模板**：包含 Frontmatter（按日期命名标题、分类 `MCRYII`、按标题自动提炼游戏/流程/人物标签）、本地封面大图、开篇引导句、属性信息表（发布时间、时长、播放量、分区、B 站链接，带 SVG 图标短代码）、引用块多行简介、前往 B 站观看按钮、底部自动整理标注。
- **图片本地化与缩略图**：自动下载高清封面保存至 `static/images/bili-<bvid小写>-cover.jpg`，并在生成文章后自动触发 `scripts/gen_thumbs.py` 在 `static/images/thumbs/` 产出 640px 缩略图。

【改哪里】
- 同步脚本 → `scripts/sync_bili_posts.py`
- 缩略图脚本 → `scripts/gen_thumbs.py`
- 生成的文章 → `content/posts/bili-<bvid小写>.md`
- 封面及缩略图 → `static/images/bili-<bvid小写>-cover.jpg` 与 `static/images/thumbs/`

【注意点】
- 脚本纯用 Python 标准库（`urllib.request` + `json`），无需安装第三方爬虫库；终端输出已做 UTF-8 转码容错，兼容标题带 emoji 的视频。
- 视频简介中的回车换行已自动转为 Markdown 引用块续行符（`\`），确保引用排版正常。
- 同步完成后需执行 `hugo --minify` 重新构建即可在首页/文章列表查看。

### 31. 评论系统（Cusdis）

【功能】轻量开源、注重隐私的 Cusdis 评论系统：
- 挂载于文章详情页（`posts/single.html`、`_default/single.html`）与留言板页（`guestbook.html`），支持在文章 Front Matter 中设置 `comments: false` 单独关闭；
- **全站配色实时同步**：评论组件作为同源 iframe 渲染，脚本动态读取当前页面主题变量（深浅色及 6 套调色板强调色）直接注入 iframe 内部，跟随切换无需刷新；
- **高度自动撑开**：通过 MutationObserver 实时监听评论区 DOM 变动与内容展开，自动调整 iframe 高度，页面无多余嵌套滚动条；
- **PJAX 生命周期集成**：换页时自动清理校准定时器与观察器，防止内存泄漏。

【改哪里】
- 评论模板、样式注入与自适应逻辑 → `layouts/partials/comments.html`
- 挂载页面 → `layouts/posts/single.html`、`layouts/_default/single.html`、`layouts/_default/guestbook.html`
- 单篇文章开关 → 文章头部 Front Matter 增加 `comments: false`（默认缺省为 true 开启）

【注意点】
- Cusdis 依靠 `data-app-id`（`8f6eb360-92ed-4eb3-92a0-d360c266affe`）识别站点，评论数据托管在 Cusdis 服务端，审核与通知需前往 Cusdis 控制台；
- iframe 样式注入强制带有 `:root { color-scheme: dark/light !important; }`，必须保留 `!important` 避免 Chrome 底层将透明 iframe 错误涂白；
- 换页清理依赖 `window.MCRYII_PageUnsubscribers`，若后续调整 PJAX 逻辑需确保该清理链不断开。

### 32. WSL2 管理与运行环境

【功能】项目整体已迁移至 WSL2（`Ubuntu-24.04`）原生 Linux 文件系统（`/home/mcryii/myblog-new`），享受 Linux 原生 I/O 的极速 Hugo 构建与 LiveReload。提供统一的 Shell 管理工具链，支持 Windows 浏览器与快捷方式透明协同。
- **极速构建**：Linux 原生文件系统下构建速度比 Windows 挂载盘（`/mnt/d`）快 5~10 倍（全站构建约 200ms）。
- **一键管理**：提供 `./run.sh` 与 `./manage.sh`，统一串联缩略图生成、版本号自动递增、搜索索引检测与 Hugo 启动。
- **无缝协同**：WSL 镜像网络与 Windows 端口互通；Windows 快捷方式（`MCRYII.lnk`）与 `打开博客.ps1` 自动桥接 WSL 后台，保持双击即开体验。

【改哪里】
- WSL 原生启动脚本 → `run.sh`
- WSL 综合管理脚本 → `manage.sh`（常用子命令：`serve`、`build`、`thumbs`、`sync-bili`、`lunar`、`tool`、`status`）
- 自动版本递增工具 → `scripts/bump_version.py`
- 本地机器路径与端口配置 → `config.local.json`（`blog_root: "/home/mcryii/myblog-new"`）
- Windows 协同脚本 → `打开博客.ps1`、`打开博客.bat`

【注意点】
- 项目主代码存放在 WSL Linux 原生目录 `/home/mcryii/myblog-new`，Windows 资源管理器可通过 `\\wsl.localhost\Ubuntu-24.04\home\mcryii\myblog-new` 访问，或映射为网络驱动器；
- 原 Windows 目录 `D:\Downloads\Programs\myblog-new` 完整保留作为本地物理备份；
- WSL 内部已独立配置 Git 用户名/邮箱以及 SSH 密钥（`~/.ssh/id_ed25519`，权限 `600`），严格遵循“本地优先、禁自动推送”原则；
- Linux 下脚本必须保持 Unix LF 换行符与 `chmod +x` 可执行权限；`run.sh` 传入 `--daemon` 可后台静默运行；
- `打开博客.ps1` 使用 Windows 原生 `curl.exe` 对 `http://localhost:$port/` 进行就绪健康探测（超时 0.5s），避免在系统代理或 WSL NAT 环境下因硬编码 IPv4 (`127.0.0.1`) 导致与 IPv6 (`[::1]`) 端口映射不匹配而出现数十秒的假死重试超时；若修改探测逻辑需确保兼容 IPv4/IPv6 双栈。

---

## 关键注意点（汇总）

1. **顶栏菜单**：硬编码在 `layouts/partials/header.html` 的 `$navItems`，hugo.yaml 的 `menu.main` 没用上。
2. **主页布局**：`layouts/index.html` 只显示欢迎语、不显示文章列表。（原备用的 `data/steam.json` 与 `static/images/skills/` 已删除）
3. **PJAX 局部刷新**：站内跳转只替换 `<main>` 内容，页面级脚本会重新执行；`global-nav.js` 启动时会给首次整页加载的历史记录 `replaceState` 打上 `{pjax:true}` 标记（否则 PJAX 跳转后按浏览器"返回"会因 `e.state` 为空导致 URL 与内容错位），新增历史入口逻辑时别破坏这一点；挂在 `<main>` 外的页面级弹层（如动态灯箱）会在换页时由 `global-nav.js` 清理；首页滚动、阅读进度条等页面级滚动监听由 `static/js/scroll-effects.js` 统一注册，并在 PJAX 换页前清理，避免反复切换后监听器累积。播放器状态跨页保持，别把它写进会重复执行的代码里。局部刷新请求有 5 秒超时兜底，网络卡住/失败时自动转整页跳转，且卡住期间再次点击会直接整页跳转，不会无限等待。页面模板里新增 document/window 级监听器时，必须注册到 `window.MCRYII_PageUnsubscribers`（由 `scroll-effects.js` 提供、`global-nav.js` 换页前统一清理），否则会跨页泄漏累积。搜索结果被点击跳转时会自动关闭搜索弹窗（弹窗挂在 `<main>` 外，PJAX 不清理它，关掉是防止旧结果残留到新页面）。
4. **改完要构建**：纯改 `content/`、`data/`、`static/` 后需重新 `hugo` 构建；播放器歌单、下载列表、搜索索引、日历数据都是构建期自动生成的。
5. **localStorage 记忆**：导航解锁、规划"开始"状态、播放器状态/歌单/播放模式、日历日程、下载页折叠状态、一言每日缓存都存在浏览器本地，换设备/浏览器会重置（首页打字机用 sessionStorage，关标签页即清）。
6. **本地优先**：所有修改仅限本地，禁止未经批准推送 GitHub。
7. **成人内容书签在本地**：`static/bookmarks-local.json` 已被 `.gitignore` 忽略，仅本地预览合并显示；修改时注意不要把它提交到 GitHub。
8. **视觉令牌与字体集中在 extend_head.html**：全站配色/圆角/阴影的设计令牌（`--gold`、`--line`、`--card-bg-dark`、`--radius-*`、`--shadow-rest/pop`、`--ease-out` 等）定义在顶栏样式块的 `:root` 与 `html[data-theme="dark"]` 覆盖里，改配色先改令牌。全站中文使用霞鹜文楷（`LXGW WenKai Screen` Webfont 分块按需加载），西文数字走现代系统栈；卡片统一采用 `18px` 平滑大圆角与多层弥散阴影（暗色带有 `0 0 0 1px` 微内发光边缘）；约定：卡片**静止态描边一律用 `var(--line)`（中性发丝线）**，金色只做点睛（hover、激活态、小徽章、图标、数字）；各页面模板里新增卡片样式请遵守同一约定。
